inline
fn u256_to_tab(inline u256 data) -> inline u32[8] {
  inline u32[8] tab;
  inline u256 rotated_data;
  inline int i;

  rotated_data = data;

  for i=0 to 7 {
    tab[i] = (u32)[data]
    x[i] = rotated_first_tab & 0xFF;
    rotated_first_tab >>= 32;
  }

  return tab;

}
